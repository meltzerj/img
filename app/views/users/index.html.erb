<h1 style="font-size:25px;text-align:center;"><b>IMG is a University of Pennsylvania tech startup community that meets regularly to develop innovative products and businesses that aim to make a broad impact.</b></h1><br />

<p style="font-size:15px;color:#004782;">Our members live and breathe entrepreneurship. They worship technology. We look for pain points in life that can be addressed by blah blah blah. If you are interested in helping IMG grow its vision please</p><br />

<%= content_tag(:p, notice, :class => 'notice') if notice.present? %>
<%= content_tag(:p, alert, :class => 'alert') if alert.present? %>

<p style="font-size:20px;float:left;margin-right:10px;color:#004782;"><b>Sign up here:</b></p>

<%= form_for @email, :url => emails_path, :class => "email-form" do |f| %>
	<%= f.text_field :email, :placeholder => "email address", :class => "field" %>
	<%= f.submit "Submit" %>
<% end %>

<div id="projects-div">
	<h1 style="font-size 25px;margin-bottom:10px;">IMG Projects and Businesses</h1>
	<% @users.each do |user| %>
	    <div class="project-frame">
		    <a href="<%= user.project_dest %>"><img class="center" src="<%= user.project %>" /></a>
		    <div class="project-info">
		        <div class="text"><b><%= user.project_name %></b> by </div>
		        <div class="gravatar"><a href="<% user.gravatar_dest %>"><img class="gravatar-image" title="<%= user.name %>" src="<%= user.gravatar %>" /></a></div>
		    </div>
		</div>
	<% end %>
</div>		